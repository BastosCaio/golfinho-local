[theme]
primaryColor="#818181"  #CHANGE_APP_COLOR
# primaryColor="#A9A9A9"
backgroundColor="#4c4b57"
secondaryBackgroundColor="#0C1419"
textColor="#A9A9A9"
font = "sans serif"
base = "light"

# this is needed for local development with docker
[server]
# if you don't want to start the default browser:
headless = true
# you will need this for local development:
runOnSave = true
# you will need this if running docker on windows host:
fileWatcherType = "poll"